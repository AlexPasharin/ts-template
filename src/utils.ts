/**
 * Given two numbers returns their sum
 *
 * @param a first number
 * @param b second number
 * @returns sum of parameters
 *
 * @example myFunction(2, 3) returns 5
 */
export const sum_sum = (a: number, b: number): number => a + b;
